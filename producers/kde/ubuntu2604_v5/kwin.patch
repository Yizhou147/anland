diff -ruN a/src/inputmethod.cpp b/src/inputmethod.cpp
--- a/src/inputmethod.cpp	2026-05-12 18:26:46.000000000 +0800
+++ b/src/inputmethod.cpp	2026-06-24 13:22:39.044705903 +0800
@@ -615,6 +615,16 @@
     }
 }
 
+void InputMethod::commitText(const QString &text)
+{
+    if (text.isEmpty()) {
+        return;
+    }
+    // Same entry point the zwp_input_method_v1 context's commitString signal is
+    // wired to (see adoptInputMethodContext): KWin decides how to deliver it.
+    commitString(m_serial++, text);
+}
+
 void InputMethod::commitString(qint32 serial, const QString &text)
 {
     if (auto t1 = waylandServer()->seat()->textInputV1(); t1 && t1->isEnabled()) {
diff -ruN a/src/inputmethod.h b/src/inputmethod.h
--- a/src/inputmethod.h	2026-05-12 18:26:46.000000000 +0800
+++ b/src/inputmethod.h	2026-06-24 13:19:46.130416897 +0800
@@ -71,6 +71,13 @@
 
     void commitPendingText();
 
+    // Inject committed text from a compositor-side input source that does not
+    // speak zwp_input_method_v1 (e.g. the anland Android consumer's keyboard).
+    // Enters KWin's routing at the same point a real IME's commit_string does:
+    // delivered to the focused text-input client, or synthesized as key events
+    // when the client has no text-input interface enabled.
+    void commitText(const QString &text);
+
     // for use by the QPA
     InternalInputMethodContext *internalContext() const
     {
diff -ruN a/src/backends/CMakeLists.txt b/src/backends/CMakeLists.txt
--- a/src/backends/CMakeLists.txt	2026-05-12 18:26:46.000000000 +0800
+++ b/src/backends/CMakeLists.txt	2026-06-21 16:16:45.502196971 +0800
@@ -1,3 +1,4 @@
+add_subdirectory(anland)
 add_subdirectory(drm)
 add_subdirectory(fakeinput)
 add_subdirectory(libinput)
diff -ruN a/src/main_wayland.cpp b/src/main_wayland.cpp
--- a/src/main_wayland.cpp	2026-05-12 18:26:46.000000000 +0800
+++ b/src/main_wayland.cpp	2026-06-21 16:16:45.502327729 +0800
@@ -10,6 +10,7 @@
 
 #include "config-kwin.h"
 
+#include "backends/anland/anland_backend.h"
 #include "backends/drm/drm_backend.h"
 #include "backends/virtual/virtual_backend.h"
 #include "backends/wayland/wayland_backend.h"
@@ -357,6 +358,7 @@
                                      i18n("Exits this instance so it can be restarted by kwin_wayland_wrapper."));
 
     QCommandLineOption drmOption(QStringLiteral("drm"), i18n("Render through drm node."));
+    QCommandLineOption anlandOption(QStringLiteral("anland"), i18n("Render to the anland display daemon."));
     QCommandLineOption locale1Option(QStringLiteral("locale1"), i18n("Extract locale information from locale1 rather than the user's configuration"));
 
     QCommandLineParser parser;
@@ -380,6 +382,7 @@
     parser.addOption(scaleOption);
     parser.addOption(outputCountOption);
     parser.addOption(drmOption);
+    parser.addOption(anlandOption);
     parser.addOption(locale1Option);
     parser.addOption(fullscreenOption);
 
@@ -444,6 +447,7 @@
 #endif
         Wayland,
         Virtual,
+        Anland,
     };
 
     BackendType backendType;
@@ -452,7 +456,9 @@
     qreal outputScale = 1;
 
     // Decide what backend to use.
-    if (parser.isSet(drmOption)) {
+    if (parser.isSet(anlandOption)||qEnvironmentVariableIsSet("ANLAND_SOCKET")) {
+        backendType = BackendType::Anland;
+    } else if (parser.isSet(drmOption)) {
         backendType = BackendType::Kms;
 #if KWIN_BUILD_X11
     } else if (parser.isSet(x11DisplayOption)) {
@@ -562,6 +568,14 @@
         }));
         break;
     }
+    case BackendType::Anland: {
+        // The display-daemon socket path comes from $ANLAND_SOCKET; the backend
+        // falls back to its built-in default (/tmp/display_daemon.sock) if unset.
+        const QString anlandSocket = qEnvironmentVariable("ANLAND_SOCKET");
+        a.setSession(KWin::Session::create(KWin::Session::Type::Noop));
+        a.setOutputBackend(std::make_unique<KWin::AnlandBackend>(anlandSocket));
+        break;
+    }
     }
 
     KWin::WaylandServer *server = KWin::WaylandServer::create();
diff -ruN a/src/wayland/transaction.cpp b/src/wayland/transaction.cpp
--- a/src/wayland/transaction.cpp	2026-05-12 18:26:46.000000000 +0800
+++ b/src/wayland/transaction.cpp	2026-08-25 23:30:00.000000000 +0800
@@ -267,6 +267,17 @@
 void Transaction::watchDmaBuf(TransactionEntry *entry)
 {
 #if defined(Q_OS_LINUX)
+    // The anland backend always composites client buffers with EGL; it never
+    // scans them out directly. EGL's implicit dma-buf synchronization waits
+    // before the texture is sampled, so exporting a sync_file and installing a
+    // QSocketNotifier for every mailbox commit only duplicates that wait. On
+    // fast clients this otherwise adds poll/ioctl/close work to every commit and
+    // delays wl_buffer.release. Keep the generic KWin behavior unless the
+    // anland-only startup flag explicitly opts into this composited fast path.
+    if (qEnvironmentVariableIntValue("ANLAND_SKIP_IMPLICIT_SYNC_WAIT") == 1) {
+        return;
+    }
+
     const DmaBufAttributes *attributes = entry->buffer->dmabufAttributes();
     if (!attributes) {
         return;
diff -ruN a/src/scene/surfaceitem.cpp b/src/scene/surfaceitem.cpp
--- a/src/scene/surfaceitem.cpp	2026-05-12 18:26:46.000000000 +0800
+++ b/src/scene/surfaceitem.cpp	2026-08-25 23:30:00.000000000 +0800
@@ -133,15 +133,22 @@
 void SurfaceItem::addDamage(const Region &region)
 {
+    // Mailbox clients commonly replace a fully damaged buffer hundreds of times
+    // before the next composite. The latest buffer still gets installed through
+    // bufferChanged; repeating damage mapping and repaint propagation is redundant.
+    const bool damageAlreadyPending = !region.isEmpty() && m_damage.contains(region.boundingRect());
     if (m_lastDamage) {
         const auto diff = std::chrono::steady_clock::now() - *m_lastDamage;
         m_lastDamageTimeDiffs.push_back(diff);
         if (m_lastDamageTimeDiffs.size() > 100) {
             m_lastDamageTimeDiffs.pop_front();
         }
         m_frameTimeEstimation = std::accumulate(m_lastDamageTimeDiffs.begin(), m_lastDamageTimeDiffs.end(), 0ns) / m_lastDamageTimeDiffs.size();
     }
     m_lastDamage = std::chrono::steady_clock::now();
     m_damage += region;
-
+    if (damageAlreadyPending) {
+        return;
+    }
+
     const RectF sourceBox = m_bufferToSurfaceTransform.map(m_bufferSourceBox, m_bufferSize);
     const qreal xScale = sourceBox.width() / m_destinationSize.width();
